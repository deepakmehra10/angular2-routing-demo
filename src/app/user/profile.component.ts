import {Component} from '@angular/core';

@Component({
  selector: 'app-root',
  template: `<h1>Edit your profile</h1>
  <hr>
  <div>
    <button>Save</button>
    <button>Cancel</button>
  </div>
  `
})

export class ProfileComponent {
}
